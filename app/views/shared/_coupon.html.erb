<li>
  <h2> coupon.name </h2>
  <% if current_user.admin? %>
  | <%= link_to "delete", coupon, :method => :delete, :confirm => "You sure?",
                                :title => "Delete #{coupon.name}" %>
  <% end %>
</li>
